# 📱 카카오톡 일간/주간 주식 리포트 자동 발송 설정 가이드

본 가이드는 매일/매주 작성된 주식 투자 분석 리포트를 카카오톡 **"나와의 채팅방"**으로 자동 전송하는 방법입니다.

---

## 1단계: 카카오 디벨로퍼스 앱 생성 및 REST API 키 발급

1. **[카카오 디벨로퍼스](https://developers.kakao.com)** 웹사이트 접속 및 로그인
2. 상단 메뉴의 **[내 애플리케이션]** -> **[애플리케이션 추가하기]** 클릭
   - 앱 이름: `StockReport` (임의 지정 가능)
   - 사업자명: `Personal` (임의 지정 가능)
3. 생성된 앱 선택 후 **[앱 키]** 항목의 **`REST API 키`** 값을 복사합니다.

---

## 2단계: 플랫폼 등록 및 메시지 권한 설정

1. **Redirect URI 설정:**
   - 왼쪽 메뉴 **[앱 설정]** -> **[플랫폼]** 클릭
   - Web 플랫폼 등록 -> 사이트 도메인에 `http://localhost:5000` 입력 후 저장
   - 왼쪽 메뉴 **[제품 설정]** -> **[카카오 로그인]** 클릭
   - 카카오 로그인 활성화 상태를 **[ON]**으로 변경
   - 하단 **[Redirect URI 등록]** 버튼 클릭 -> `http://localhost:5000/oauth` 입력 후 저장

2. **메시지 전송 동의항목 설정:**
   - 왼쪽 메뉴 **[제품 설정]** -> **[카카오 로그인]** -> **[동의항목]** 클릭
   - **[카카오톡 메시지 전송 (talk_message)]** 항목 찾기 -> 우측 `설정` 클릭
   - 동의 단계를 **[선택 동의]** 또는 **[필수 동의]**로 설정 후 사유(예: `개인 주식 리포트 알림 전송`) 작성 후 저장

---

## 3단계: 최초 토큰 발급 (1회 진행)

1. 파이썬 환경이 설치되어 있는 경우 terminal / 파워쉘에서 아래 명령어를 실행합니다:
   ```bash
   python setup_kakao_token.py
   ```
   *(또는 파워쉘 헬퍼 실행)*
   
2. 터미널에 표시되는 URL을 복사하여 웹 브라우저 주소창에 접속합니다.
3. 카카오 로그인 및 메시지 전송 동의 진행 후 이동되는 브라우저 주소창 URL을 확인합니다:
   - 주소 예시: `http://localhost:5000/oauth?code=YOUR_AUTHORIZATION_CODE...`
4. 주소창의 `code=` 뒷부분 값(인가 코드)을 복사하여 터미널에 입력합니다.
5. `kakao_config.json`에 Access Token과 Refresh Token이 자동 발급되어 저장됩니다.

---

## 4단계: 테스트 및 즉시 전송

미리보기 테스트 (실제 카카오톡 전송 없이 포맷 확인):
```bash
python send_kakao_report.py --dry-run
```

실제 카카오톡 전송:
```bash
python send_kakao_report.py
```

---

## 5단계: 매일 아침 자동 전송 설정 (Windows 작업 스케줄러)

매일 아침(예: 08:00) 지정된 시간에 카카오톡으로 주식 리포트를 받기 위해 Windows 작업 스케줄러에 등록합니다.

1. `Win + R` 누른 후 `taskschd.msc` 입력하여 **작업 스케줄러** 실행
2. 우측 **[작업 만들기]** 클릭
   - 이름: `Stock_Kakao_Report_Daily`
3. **[트리거]** 탭 -> [새로 만들기]
   - 작업 시작: `매일`
   - 시작 시간: `오전 08:00:00`
4. **[동작]** 탭 -> [새로 만들기]
   - 동작: `프로그램 시작`
   - 프로그램/스크립트: `python` (또는 파이썬 실행 절대 경로)
   - 인수 추가: `send_kakao_report.py`
   - 시작 위치: `c:\Users\adi5s\OneDrive\Documents\STOCK`
5. [확인]을 클릭하여 등록 완료.

> [!NOTE]
> 한번 토큰이 설정되면 `Refresh Token`을 통해 Access Token이 자동 갱신되므로 별도의 재인증 절차 없이 무기한 자동 전송됩니다.
